<%- include('../includes/head.ejs') %>

</head>

<%- include('../includes/navigation.ejs') %>

<body>
    <div class="container text-center">
        <div class="d-flex justify-content-center">
          <div class="row mx-auto">
            <h1 class="mb-4 pb-4"><%=pageTitle %></h1>


            <div class="bg-danger font-weight-bold">Use email "test@test.com" with password "test" for access to demo data</div>
    <div class="bg-light container border border-secondary rounded">

        <div class="mt-3 container text-center">
            <div class="row">
              <div class="col">

        <form class="bg-light rounded row gx-3 gy-2 pb-4 mb-4" action="/worthie/login" method="POST">

            <div class="row-sm-1">
                <label class="text-muted" for="email">Email</label>
                <% if (isIncorrectLoginUsed === "true") { %>    
                  <h5 class="text-danger">Email/password combination incorrect or Email not registered</h5>
                <% } %>
                <input
                  type="email"
                  class="form-control"
                  name="email"
                  id="email"
                  placeholder="email"
                />
              </div>

              <div class="row-sm-3">
                <label class="text-muted" for="password">Password</label>
                <input
                  type="password"
                  class="form-control"
                  name="password"
                  id="password"
                  placeholder="password"
                />
              </div>
              <div class="row-sm-3">

                <h5 class="text-light">Login</h5>
                <button
                  type="submit"
                  class="btn btn-primary"
                >
                  Login
                </button>
            </div>
            </div>
              </div>
              </form>